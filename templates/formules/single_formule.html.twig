<div class="product-item text-center">
    <a href="{{ path('product', { 'slug' : formule.slug }) }}"><img src="/uploads/images/{{ formule.illustration }}" alt="{{ formule.name }}" class="img-fluid"></a>
    <h5>{{ formule.name }}</h5>
    <span class="product-subtitle">{{ formule.subtitle }}</span>
    <span class="product-price">{{ (formule.price /100)|number_format(2, ',', '.')  }} € </span>
</div>